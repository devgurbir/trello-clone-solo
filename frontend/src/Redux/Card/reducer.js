import { createReducer } from "@reduxjs/toolkit";
import { actionConstants } from "./actions";

const initState = {
    // add initState
}

const cardReducer = (state = initState, action) => {
    switch(action.type){
        // add different cases and how they will change the state
    }
}

export default cardReducer